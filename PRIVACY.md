# 隐私说明

链动小铺消费助手是本地运行的开源浏览器扩展，不提供云端账户或数据同步服务。

## 处理的数据

扩展会处理并在浏览器扩展本地存储中保存：

- 商品名与商品链接
- 订单号
- 金额与数量
- 订单状态
- 下单时间
- 面板位置、尺寸和显示状态

这些数据用于页面内统计、筛选、排序和 CSV 导出。

## 不保存的数据

- 查询手机号或其他 `keywords` 原文
- 查询 `ticket`
- 验证码
- Cookie
- 卡密或权益内容
- 浏览历史

如果用户主动启用购买页快捷填写，联系方式和安全密码会写入当前浏览器的 `chrome.storage.local`，仅用于双击悬浮球时填写当前商品购买页，不会上传、导出或写入项目源码。用户可在扩展设置中清空这些值。

订单缓存键使用 `keywords` 的 SHA-256 摘要前缀进行本地隔离，但不保存原文。

## 网络请求

扩展只向用户当前打开的 `https://pay.ldxp.cn` 发送订单列表请求。请求使用页面 URL 中已有的查询凭证，并运行在站点页面主世界中。

扩展不包含第三方统计、广告、遥测或错误上报服务。

## 删除数据

用户可以通过面板菜单清空当前查询标识对应的本地订单缓存。移除扩展会清除浏览器保存的全部扩展本地数据。

## 注意事项

这是非官方工具。用户应遵守链动小铺的服务条款和当地适用法律，并只处理自己有权访问的订单数据。

本插件永久免费，不提供任何付费服务。如项目内容涉及侵权，可通过 QQ 群 `613550608` 联系作者，核实后将立即删除或调整相关内容。
