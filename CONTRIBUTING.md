# 贡献指南

感谢你改进链动小铺消费助手。这个扩展直接运行在订单页面中，修改时需要同时关注数据正确性、页面兼容性和用户隐私。

## 开发环境

- Node.js 18 或更高版本
- Chrome 或 Edge 最新稳定版
- Python 3（仅用于启动本地演示页，也可替换为任意静态文件服务器）

项目没有第三方运行时依赖，不需要执行构建命令。

```bash
npm test
npm run check
python -m http.server 8765
```

访问 `http://127.0.0.1:8765/demo.html` 进行界面验证。

## 修改原则

1. 不绕过、识别或自动填写站点验证码。
2. 不读取卡密、安全密码、Cookie、Local Storage 或与统计无关的订单详情。
3. 不把订单数据、手机号、查询 ticket 或浏览记录发送给第三方。
4. 页面与扩展之间的事件载荷必须经过字段白名单和 URL 校验。
5. 站点适配尽量集中在 `page-bridge.js` 和 `core.js`，不要把站点细节散落在 UI 中。
6. 金额计算使用整数分，避免浮点累计误差。
7. 动态页面文本在写入 `innerHTML` 前必须经过 `escapeHtml`。

## 代码结构

- `src/core.js`：可在 Node.js 中测试的纯函数。新增筛选或统计逻辑时，优先在这里实现并补测试。
- `src/page-bridge.js`：运行在页面主世界，负责观察和读取站点订单接口。此文件应保持小而保守。
- `src/content.js`：运行在扩展隔离世界，负责本地存储、Shadow DOM 和交互。
- `src/background.js`：仅处理扩展工具栏按钮。
- `src/panel.css`：Shadow DOM 内部样式。

详细的数据流和事件协议见 [docs/architecture.md](docs/architecture.md)。

## 测试要求

每个 Pull Request 至少完成：

```bash
npm test
npm run check
```

涉及界面的修改还应手动验证：

- 首次打开、收起、隐藏和通过工具栏恢复
- 悬浮球与面板拖动
- 面板右下角缩放与刷新后尺寸恢复
- 390×800 和常规桌面视口无横向溢出
- 关键字、日期、状态和金额筛选
- 时间与金额四种排序
- 商品链接在新标签页打开且仅指向 `pay.ldxp.cn/item/`
- 后台读取订单时页面页码不变化
- CSV 能被 Excel 正确识别为 UTF-8

## 站点适配

站点接口或 DOM 可能随时变化。适配时：

1. 只检查自己有权访问的订单页面。
2. 在提交内容中移除手机号、ticket、订单号和商品敏感信息。
3. 优先更新 `docs/architecture.md` 中的接口约定和选择器说明。
4. 保留 DOM 解析作为当前页同步的兜底能力。
5. 不在测试夹具中加入真实订单。

## 提交 Pull Request

- 使用清晰、单一目的的提交。
- 描述用户可见变化、站点兼容性影响和验证方式。
- 行为变更应更新 README 或架构文档。
- 新功能应补充测试；无法自动化的部分应列出手动验证结果。
